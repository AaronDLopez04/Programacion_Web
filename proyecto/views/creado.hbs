    <style>
        .card {
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .container {
            max-width: 800px;
            margin-top: 50px;
        }

        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
        }

        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #004085;
        }
    </style>
</head>
<body>

<header class="bg-dark text-white py-3">
    <div class="container text-center">
        <h2>¡Videojuego Guardado Exitosamente!</h2>
    </div>
</header>

<div class="container">
    <div class="card my-5">
        <div class="card-body">
            <h5 class="card-title text-center">Detalles del Videojuego Guardado</h5>
            <hr>
            <p><strong>ID:</strong> {{nuevo.id}}</p>
            <p><strong>Título:</strong> {{nuevo.titulo}}</p>
            <p><strong>Género:</strong> {{nuevo.genero}}</p>
            <p><strong>Plataforma:</strong> {{nuevo.plataforma}}</p>
            <p><strong>Precio:</strong> ${{nuevo.precio}}</p>
            <p><strong>Clasificación por edad:</strong> {{nuevo.clasificacion_edad}}</p>
            
            <div class="d-flex justify-content-center mt-4">
                <a href="/" class="btn btn-success me-3">Volver al Inicio</a>
                <a href="/videojuegos" class="btn btn-primary">Ver Catálogo</a>
            </div>
        </div>
    </div>
</div>